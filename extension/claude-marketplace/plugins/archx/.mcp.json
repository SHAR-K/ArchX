{
  "mcpServers": {
    "archx": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/archx-mcp.mjs"],
      "env": {
        "ARCHX_PROJECT_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
